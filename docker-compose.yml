services:
  dev: 
    build: .
    entrypoint: bash -c "cd /site && bundle exec jekyll s --host 0.0.0.0 -w --drafts --future"
    ports:
      - "4000:4000"
    volumes:
      - "$PWD:/site"
      - bundle_cache:/usr/local/bundle

volumes:
  bundle_cache: